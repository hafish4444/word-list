import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Vocabulary Master - Learn English Words",
  description:
    "Enhance your English vocabulary with our interactive quiz featuring definitions, examples, and translations in Thai and Japanese.",
  keywords: "vocabulary, English, learning, quiz, education, Thai, Japanese, translations",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
